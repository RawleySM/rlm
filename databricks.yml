bundle:
  name: rlm_adk_bundle

artifacts:
  rlm_adk_wheel:
    type: whl
    build: uv build
    path: .

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-6983841757863745.5.azuredatabricks.net
      root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    run_as:
      user_name: ${workspace.current_user.userName}
